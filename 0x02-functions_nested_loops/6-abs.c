#include "main.h"

/**
 * _abs - Compute the absolute value of an integer
 * @n: the int in question
 * Return: the abs of the integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
