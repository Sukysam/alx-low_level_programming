#include "main.h"

/**
 * mul - A function to multiply tow veriables
 * @a: First parameter
 * @b: Second Parameter
 * Return: (0)
 */

int mul(int a, int b)
{
	int multiply = a * b;

	return (multiply);
}
